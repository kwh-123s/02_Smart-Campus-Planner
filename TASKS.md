# TASKS: Smart Campus Planner 단계별 구현 계획

## 0. 문서 개요
- 이 문서는 `PRD.md`에 정의된 요구사항과 MVP 범위를 바탕으로, Smart Campus Planner를 한 번에 완성하지 않고 **3단계**로 나누어 구현하기 위한 작업 계획서다.
- 각 단계는 이전 단계에서 만든 결과물 위에 새로운 기능을 쌓아 올리는 방식으로 진행된다. 즉, 1단계가 끝나야 2단계를 시작할 수 있고, 2단계가 끝나야 3단계를 시작할 수 있다.
- 이 문서에는 실제 프로그램 코드는 포함하지 않으며, "무엇을 만들 것인지"에 대한 계획만 정리한다.

## 전체 진행 순서 한눈에 보기
| 단계 | 이름 | 핵심 목표 |
|---|---|---|
| 1단계 | 할 일 목록의 기본 뼈대 만들기 | 할 일을 추가하고, 완료 체크하고, 삭제할 수 있는 가장 기본적인 화면 완성 |
| 2단계 | 할 일 분류와 진행률 표시 기능 추가 | 카테고리(학과/동아리/개인), 중요도(상/중/하), 마감일, 전체 완료율 표시 추가 |
| 3단계 | 새로고침해도 유지되는 저장 기능 완성 | 입력한 모든 데이터가 새로고침·재접속 후에도 사라지지 않도록 저장 기능 적용 및 전체 점검 |

---

## 1단계: 할 일 목록의 기본 뼈대 만들기

### 비전공자를 위한 쉬운 설명
할 일을 적을 수 있는 "빈 노트"부터 만드는 단계다. 아직 학과/동아리/개인 구분이나 중요도, 마감일 같은 세부 항목은 없지만, "할 일을 적고, 다 했으면 체크하고, 필요 없으면 지우는" 가장 기본적인 기능이 동작하는 화면을 완성한다. 이 단계가 끝나면 사용자는 제목만 있는 간단한 할 일 목록을 실제로 사용해볼 수 있다.

### 이 단계에서 구현할 내용
- 할 일 목록을 보여줄 기본 화면(레이아웃) 구성
- 할 일 제목을 입력해서 새로 추가하는 기능
- 추가한 할 일이 목록에 나타나는 기능
- 할 일을 완료로 체크(또는 다시 미완료로 되돌리기)하는 기능
- 할 일을 목록에서 삭제하는 기능

### 이 단계에서 다루지 않는 내용 (다음 단계에서 진행)
- 카테고리(학과/동아리/개인) 구분
- 중요도(상/중/하) 구분
- 마감일 지정
- 전체 완료율 표시
- 새로고침 후 데이터 유지(저장 기능)

### 완료 확인 기준
- 화면에서 할 일 제목을 입력해 목록에 추가할 수 있다.
- 목록에 있는 할 일을 완료 체크할 수 있고, 체크 상태가 화면에 구분되어 보인다.
- 목록에 있는 할 일을 삭제할 수 있다.
- (단, 이 단계에서는 새로고침 시 데이터가 사라져도 정상이다. 저장 기능은 3단계에서 추가한다.)

---

## 2단계: 할 일 분류와 진행률 표시 기능 추가

### 비전공자를 위한 쉬운 설명
1단계에서 만든 "빈 노트"에 이제 상세 정보를 적을 수 있는 칸을 추가하는 단계다. 어떤 할 일이 학과 일인지, 동아리 일인지, 개인 일인지 구분하고, 얼마나 중요한지(상/중/하), 언제까지 해야 하는지(마감일)를 표시할 수 있게 된다. 또한 전체 할 일 중 몇 %를 끝냈는지 한눈에 볼 수 있는 진행률 표시도 이 단계에서 추가된다.

### 이 단계에서 구현할 내용
- 할 일 추가 시 카테고리(학과 / 동아리 / 개인) 선택 기능
- 할 일 추가 시 중요도(상 / 중 / 하) 선택 기능
- 할 일 추가 시 마감일(날짜) 입력 기능
- 목록에서 각 할 일의 카테고리, 중요도, 마감일이 함께 보이도록 화면 표시 개선
- 전체 할 일 개수 대비 완료된 할 일 개수의 비율(완료율)을 계산해 화면 상단 등에 표시

### 이 단계에서 다루지 않는 내용 (다음 단계에서 진행)
- 새로고침이나 재접속 후에도 데이터가 유지되는 저장 기능 (2단계까지는 아직 새로고침 시 데이터가 사라질 수 있음)

### 완료 확인 기준
- 할 일을 추가할 때 카테고리, 중요도, 마감일을 함께 지정할 수 있다.
- 목록에서 각 할 일의 카테고리·중요도·마감일을 구분해서 확인할 수 있다.
- 화면에서 "전체 할 일 중 몇 %가 완료되었는지" 수치가 표시되고, 완료 체크를 하거나 취소하면 그 값이 즉시 바뀐다.

---

## 3단계: 새로고침해도 유지되는 저장 기능 완성 및 전체 점검

### 비전공자를 위한 쉬운 설명
지금까지 만든 기능은 브라우저를 새로고침하면 적어둔 내용이 다 사라지는 "메모지" 같은 상태였다. 이 단계에서는 적어둔 할 일이 새로고침을 하거나 창을 닫고 다시 열어도 그대로 남아있도록 "저장" 기능을 추가한다. 즉, 1~2단계에서 만든 기능은 그대로 두고, 그 위에 "기억하는 기능"을 얹는 단계다. 마지막에는 지금까지 만든 모든 기능(추가/분류/완료율/저장)이 서로 잘 어우러져 작동하는지 전체적으로 확인한다.

### 이 단계에서 구현할 내용
- 할 일을 추가, 완료 체크, 삭제할 때마다 그 결과가 자동으로 저장되는 기능
- 페이지를 새로고침하거나 브라우저를 다시 열었을 때, 저장된 할 일 목록(제목, 카테고리, 중요도, 마감일, 완료 여부 포함)을 그대로 불러와 화면에 다시 보여주는 기능
- 1~2단계에서 만든 전체 기능(추가/카테고리/중요도/마감일/완료 체크/삭제/완료율 표시)이 저장 기능과 함께 정상적으로 동작하는지 확인하는 최종 점검

### 완료 확인 기준
- 할 일을 몇 개 추가하고 일부는 완료 체크한 뒤 새로고침해도, 추가했던 할 일과 완료 상태, 카테고리·중요도·마감일 정보가 그대로 남아있다.
- 브라우저 탭이나 창을 닫고 다시 열어도 동일하게 데이터가 유지된다.
- PRD에서 정의한 MVP 기능(할 일 추가, 카테고리 설정, 중요도 설정, 마감일 지정, 완료 체크 및 삭제, 전체 완료율 표시, 저장 유지)이 모두 정상 동작한다.

---

## 참고 사항
- 본 문서는 계획 수립 단계이며, 각 단계의 실제 구현(코드 작성)은 이후 별도로 진행한다.
- 각 단계는 앞 단계의 결과물을 계속 사용하며 기능을 덧붙이는 구조이므로, 단계를 건너뛰지 않고 순서대로 진행하는 것을 권장한다.
