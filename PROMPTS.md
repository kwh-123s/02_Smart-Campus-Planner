# PROMPTS: Smart Campus Planner 단계별 구현 요청 프롬프트

## 0. 문서 개요
- 이 문서는 `PRD.md`(프로젝트 목적/요구사항/MVP 범위)와 `TASKS.md`(1~3단계 작업 계획)를 바탕으로, 실제 구현을 요청할 때 사용할 수 있는 프롬프트 3개(T1, T2, T3)를 정리한 것이다.
- T1 → T2 → T3 순서로 **한 단계씩** 진행한다. 앞 단계의 결과물을 실제로 확인(동작 테스트)한 뒤, 문제가 없을 때만 다음 단계 프롬프트를 사용한다.
- 각 프롬프트는 비전공자도 그대로 복사해서 AI 코딩 도구(Cline 등)에게 전달할 수 있도록, 쉬운 말로 "무엇을, 왜, 어떻게 확인할지"를 담아 작성했다.
- 이 문서 자체에는 프로그램 코드를 포함하지 않으며, "구현을 요청하는 문장"만 정리한다.

## 사용 방법
1. T1 프롬프트를 AI에게 전달해 1단계 기능을 구현시킨다.
2. 결과물이 잘 동작하는지 직접 눌러보고 확인한다 (예: 할 일 추가/체크/삭제가 잘 되는지).
3. 문제가 없으면 T2 프롬프트를 전달해 2단계 기능을 추가로 구현시킨다.
4. 다시 결과물을 확인한다.
5. 문제가 없으면 T3 프롬프트를 전달해 3단계(저장 기능 + 전체 점검)를 구현시킨다.
6. 최종적으로 새로고침 테스트까지 마치면 MVP가 완성된다.

---

## T1. 할 일 목록의 기본 뼈대 만들기 (기본 CRUD 화면)

### 이 프롬프트로 요청할 내용
> Smart Campus Planner 프로젝트의 PRD.md와 TASKS.md 중 1단계(T1) 내용을 참고해서, 할 일 관리 웹앱의 가장 기본적인 화면을 만들어줘.
>
> 다음 기능만 구현해줘:
> - 할 일 제목을 입력하고 추가 버튼을 누르면 목록에 새로운 할 일이 추가된다.
> - 추가된 할 일 목록이 화면에 보인다.
> - 각 할 일 옆에 체크박스(또는 버튼)가 있어서, 누르면 완료 상태로 표시되고 다시 누르면 완료가 취소된다. 완료된 할 일은 미완료 할 일과 시각적으로 구분되어 보인다(예: 취소선, 색상 변화 등).
> - 각 할 일 옆에 삭제 버튼이 있어서, 누르면 목록에서 그 할 일이 사라진다.
>
> 아직 카테고리(학과/동아리/개인), 중요도(상/중/하), 마감일, 완료율 표시, 새로고침 후 데이터 유지 기능은 만들지 마. 이 기능들은 다음 단계에서 추가할 예정이다.
>
> 구현이 끝나면 실제로 브라우저에서 열어서 동작을 확인할 수 있는 방법(실행 방법)도 알려줘.

### 이 단계가 끝나면 확인할 것 (비전공자용 체크리스트)
- [ ] 화면에 할 일을 입력하는 칸과 추가 버튼이 보인다.
- [ ] 할 일을 입력하고 추가하면 목록에 바로 나타난다.
- [ ] 목록의 할 일을 체크하면 완료된 것처럼 보이고, 다시 체크를 해제하면 원래대로 돌아온다.
- [ ] 목록의 할 일을 삭제하면 화면에서 바로 사라진다.
- [ ] (새로고침하면 목록이 사라지는 것은 이 단계에서는 정상이다. 걱정하지 않아도 된다.)

> 위 항목이 모두 잘 되는 것을 확인한 뒤에만 T2 프롬프트로 넘어간다.

---

## T2. 할 일 분류와 진행률 표시 기능 추가 (카테고리/중요도/마감일/완료율)

### 이 프롬프트로 요청할 내용
> 이전 단계(T1)에서 만든 Smart Campus Planner 할 일 목록 화면을 그대로 유지하면서, PRD.md와 TASKS.md의 2단계(T2) 내용에 맞춰 다음 기능을 추가해줘.
>
> 추가로 구현해줘야 하는 기능:
> - 할 일을 추가할 때, 이 할 일이 "학과 / 동아리 / 개인" 중 어디에 속하는지 선택할 수 있게 해줘.
> - 할 일을 추가할 때, 중요도를 "상 / 중 / 하" 중 하나로 선택할 수 있게 해줘.
> - 할 일을 추가할 때, 마감일(날짜)을 선택할 수 있게 해줘.
> - 목록에서 각 할 일을 볼 때, 그 할 일의 카테고리, 중요도, 마감일이 함께 보이도록 화면을 개선해줘. (예: 학과/동아리/개인은 색깔이나 태그로 구분, 중요도는 상/중/하 표시, 마감일은 날짜로 표시)
> - 화면 상단(또는 눈에 잘 보이는 곳)에 "전체 할 일 중 몇 %가 완료되었는지" 보여주는 완료율을 표시해줘. 할 일을 체크하거나 체크를 해제할 때마다 이 완료율이 바로 바뀌어야 한다.
>
> T1에서 만든 추가/완료 체크/삭제 기능은 그대로 유지하면서 위 기능들을 덧붙여줘. 새로고침 후 데이터가 유지되는 저장 기능은 아직 만들지 마. 그 기능은 다음 단계에서 추가할 예정이다.
>
> 구현이 끝나면 실제로 브라우저에서 열어서 동작을 확인할 수 있는 방법도 알려줘.

### 이 단계가 끝나면 확인할 것 (비전공자용 체크리스트)
- [ ] 할 일을 추가할 때 학과/동아리/개인 중 하나를 고를 수 있다.
- [ ] 할 일을 추가할 때 상/중/하 중요도를 고를 수 있다.
- [ ] 할 일을 추가할 때 마감일(날짜)을 지정할 수 있다.
- [ ] 목록에서 각 할 일의 카테고리·중요도·마감일이 눈에 보인다.
- [ ] 화면에 "전체 완료율"이 숫자(%)나 그래프 형태로 표시되고, 완료 체크를 하거나 취소하면 그 값이 바로 바뀐다.
- [ ] T1에서 되던 추가/완료 체크/삭제 기능도 여전히 잘 동작한다.

> 위 항목이 모두 잘 되는 것을 확인한 뒤에만 T3 프롬프트로 넘어간다.

---

## T3. 새로고침해도 유지되는 저장 기능 완성 및 전체 점검

### 이 프롬프트로 요청할 내용
> 이전 단계(T1, T2)까지 만든 Smart Campus Planner 기능을 모두 그대로 유지하면서, PRD.md와 TASKS.md의 3단계(T3) 내용에 맞춰 "저장" 기능을 추가해줘.
>
> 추가로 구현해줘야 하는 기능:
> - 할 일을 추가하거나, 완료 체크를 하거나, 삭제할 때마다 그 결과가 자동으로 저장되어야 한다.
> - 브라우저를 새로고침하거나, 탭/창을 닫고 다시 열었을 때도, 저장되어 있던 할 일 목록(제목, 카테고리, 중요도, 마감일, 완료 여부 모두 포함)이 그대로 화면에 다시 나타나야 한다.
> - 별도의 로그인이나 회원가입 없이, 이 브라우저에서 계속 데이터가 유지되도록 해줘.
>
> 이 기능을 추가한 뒤에는, 지금까지 만들어진 전체 기능(할 일 추가, 카테고리 설정, 중요도 설정, 마감일 지정, 완료 체크 및 삭제, 전체 완료율 표시, 새로고침 후 데이터 유지)이 서로 문제없이 함께 동작하는지도 한 번 점검해줘.
>
> 구현이 끝나면 실제로 브라우저에서 열어서, 새로고침해도 데이터가 유지되는지 확인할 수 있는 방법도 알려줘.

### 이 단계가 끝나면 확인할 것 (비전공자용 체크리스트)
- [ ] 할 일을 몇 개 추가하고, 일부는 완료 체크를 해본다.
- [ ] 브라우저를 새로고침해도, 추가했던 할 일과 완료 상태, 카테고리·중요도·마감일 정보가 그대로 남아있다.
- [ ] 브라우저 탭이나 창을 완전히 닫고 다시 열어도 데이터가 그대로 남아있다.
- [ ] 완료율 표시, 할 일 삭제, 카테고리/중요도/마감일 표시 등 앞 단계 기능들도 여전히 정상 동작한다.

> 위 항목이 모두 통과하면, PRD.md의 MVP 범위(5.1항)와 성공 기준(6항)이 모두 충족된 것으로 보고 MVP 구현을 완료한 것으로 간주한다.

---

## 참고 사항
- 각 프롬프트는 이전 단계의 결과물이 정상 동작하는 것을 확인한 뒤에만 다음 프롬프트를 사용하는 것을 전제로 한다. 앞 단계에 문제가 있는 상태로 다음 단계를 진행하면 문제가 누적될 수 있다.
- 프롬프트 안의 "PRD.md", "TASKS.md" 참고 문구는 AI가 이미 작성된 기획 문서와 작업 계획을 기준으로 구현하도록 맥락을 맞추기 위한 것이다.
- 본 문서 자체는 계획 문서이며, 실제 코드 작성은 이 프롬프트들을 실행 단계에서 순서대로 사용할 때 진행된다.
