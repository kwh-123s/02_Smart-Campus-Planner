# PRD: Smart Campus Planner

## 1. 문서 개요
- **문서명**: Smart Campus Planner PRD (Product Requirements Document)
- **작성일**: 2026-08-25
- **버전**: v0.1 (초안)
- **작성 목적**: 대학생용 일정/할 일 관리 웹앱 "Smart Campus Planner"의 기획 방향, 사용자, 요구사항, MVP 범위를 정의한다. 본 문서는 기획 단계 산출물이며, 구현 방법이나 작업 순서(개발 태스크, 아키텍처, 일정 등)는 포함하지 않는다.

## 2. 프로젝트 목적
대학생은 학과 과제, 동아리 활동, 개인적인 할 일을 동시에 관리해야 하지만, 이를 각각 다른 도구(메모, 캘린더 앱, 메신저 등)에 흩어서 관리하는 경우가 많아 마감일을 놓치거나 우선순위를 파악하기 어렵다.

**Smart Campus Planner**는 학과 / 동아리 / 개인 일정을 하나의 화면에서 통합적으로 관리할 수 있는 웹 기반 할 일 관리 앱을 제공하는 것을 목적으로 한다. 사용자는 카테고리와 중요도로 할 일을 구분하고, 마감일을 지정하며, 완료 여부와 전체 진행률을 한눈에 확인할 수 있다. 또한 브라우저를 새로고침하거나 재방문하더라도 입력한 데이터가 유지되어야 한다.

## 3. 대상 사용자
- **주요 사용자**: 학과 수업, 동아리 활동, 개인 일정을 함께 관리해야 하는 대학생(학부생/대학원생)
- **사용자 특징**
  - 하루 안에도 학과 과제, 동아리 회의/행사, 개인 약속 등 성격이 다른 할 일이 섞여 있다.
  - 할 일마다 중요도와 마감일이 달라 우선순위 판단이 필요하다.
  - 별도의 계정 가입이나 복잡한 설정 없이, PC/모바일 브라우저에서 빠르게 접속해 가볍게 사용하길 원한다.
  - 여러 기기보다는 개인 브라우저(단일 사용자) 환경에서 반복적으로 접속해 할 일을 확인하고 갱신한다.
- **사용 시나리오 예시**
  - 학기 초, 이번 주 과제·동아리 일정·개인 약속을 한 번에 등록해 마감일 순으로 확인한다.
  - 시험 기간에 중요도가 "상"인 학과 할 일을 우선 처리하고 완료 체크한다.
  - 하루를 마무리하며 전체 완료율을 확인해 진행 상황을 점검한다.

## 4. 주요 요구사항

### 4.1 기능 요구사항
1. **할 일 추가**
   - 사용자는 새로운 할 일(제목 등 기본 정보 포함)을 등록할 수 있다.
2. **카테고리 설정**
   - 할 일 등록/수정 시 카테고리를 다음 세 가지 중 하나로 지정할 수 있다: **학과 / 동아리 / 개인**
3. **중요도 설정**
   - 할 일 등록/수정 시 중요도를 다음 세 단계 중 하나로 지정할 수 있다: **상 / 중 / 하**
4. **마감일 지정**
   - 할 일마다 마감일(날짜)을 지정할 수 있다.
5. **완료 체크 및 삭제**
   - 할 일 목록에서 완료 여부를 체크(토글)할 수 있다.
   - 더 이상 필요 없는 할 일은 삭제할 수 있다.
6. **전체 완료율 표시**
   - 전체 할 일 대비 완료된 할 일의 비율을 화면에 표시한다.
7. **데이터 유지(저장)**
   - 브라우저를 새로고침하거나 다시 접속해도 등록된 할 일, 완료 상태 등 데이터가 유지되어야 한다.

### 4.2 비기능 요구사항
- **사용성**: 별도의 로그인/회원가입 없이 접속 즉시 사용할 수 있어야 한다.
- **반응성**: 할 일 추가/완료/삭제 등의 조작 결과가 즉시 화면에 반영되어야 한다.
- **데이터 지속성**: 서버 재시작, 브라우저 새로고침, 브라우저 재접속 상황에서도 데이터 유실이 없어야 한다.
- **접근성/호환성**: 일반적인 데스크톱 웹 브라우저에서 정상적으로 동작해야 한다.

## 5. MVP 범위

### 5.1 MVP에 포함되는 기능
- 할 일 추가 (제목, 카테고리, 중요도, 마감일 입력)
- 카테고리 설정: 학과 / 동아리 / 개인 중 선택
- 중요도 설정: 상 / 중 / 하 중 선택
- 마감일 지정 및 표시
- 할 일 완료 체크(토글)
- 할 일 삭제
- 전체 할 일 대비 완료율 표시
- 새로고침/재접속 시에도 데이터가 유지되는 저장 기능

### 5.2 MVP에서 제외되는 기능 (향후 검토 대상)
- 회원가입/로그인 및 다중 사용자 계정 관리
- 여러 기기 간 데이터 동기화(클라우드 동기화)
- 알림/리마인더(푸시, 이메일 등)
- 캘린더(월/주간) 뷰, 학사 일정 연동
- 할 일 검색, 필터링, 정렬 등 고급 조회 기능
- 카테고리/중요도 항목의 사용자 정의(커스텀) 추가
- 통계/리포트(주간·월간 완료율 추이 등)
- 다국어 지원

## 6. 성공 기준 (MVP 기준)
- 사용자가 학과/동아리/개인 카테고리와 상/중/하 중요도, 마감일을 지정해 할 일을 추가할 수 있다.
- 사용자가 할 일을 완료 처리하거나 삭제할 수 있다.
- 화면에서 전체 할 일 대비 완료율을 확인할 수 있다.
- 브라우저를 새로고침해도 이전에 등록한 할 일과 완료 상태가 그대로 유지된다.
